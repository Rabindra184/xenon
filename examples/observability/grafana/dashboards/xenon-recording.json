{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "tempo", "uid": "tempo" },
      "description": "Rate of recording group starts and add-device calls.",
      "fieldConfig": { "defaults": { "unit": "short" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true } },
      "targets": [
        {
          "datasource": { "type": "tempo", "uid": "tempo" },
          "queryType": "traceqlSearch",
          "query": "{ name = \"xenon.recording.start\" } | rate()",
          "refId": "Starts"
        },
        {
          "datasource": { "type": "tempo", "uid": "tempo" },
          "queryType": "traceqlSearch",
          "query": "{ name = \"xenon.recording.add_device\" } | rate()",
          "refId": "AddDevice"
        }
      ],
      "title": "Recording attempts — rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "tempo", "uid": "tempo" },
      "description": "Failed recording attempts grouped by fail_reason. The taxonomy is locked: device_busy / concurrency_cap / spawn_failed / ffmpeg_stop_failed / server_restart.",
      "fieldConfig": { "defaults": { "unit": "short" } },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "table", "placement": "right", "showLegend": true } },
      "targets": [
        {
          "datasource": { "type": "tempo", "uid": "tempo" },
          "queryType": "traceqlSearch",
          "query": "{ name = \"xenon.recording.start\" && status = error } | count_over_time() by (span.xenon.recording.fail_reason)",
          "refId": "A"
        }
      ],
      "title": "Recording failures — by reason",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "tempo", "uid": "tempo" },
      "description": "Distribution of group sizes at start. Small groups dominating = mostly single-device runs. Large groups = mosaic-driven workloads.",
      "fieldConfig": { "defaults": { "unit": "short" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true } },
      "targets": [
        {
          "datasource": { "type": "tempo", "uid": "tempo" },
          "queryType": "traceqlSearch",
          "query": "{ name = \"xenon.recording.start\" } | count_over_time() by (span.xenon.recording.device_count)",
          "refId": "A"
        }
      ],
      "title": "Group size distribution",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "tempo", "uid": "tempo" },
      "description": "Recording stop events labeled by composite_enabled. Useful for capacity planning — composites are 2x ffmpeg load.",
      "fieldConfig": { "defaults": { "unit": "ms" } },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true } },
      "targets": [
        {
          "datasource": { "type": "tempo", "uid": "tempo" },
          "queryType": "traceqlSearch",
          "query": "{ name = \"xenon.recording.stop\" } | quantile_over_time(duration, 0.99)",
          "refId": "A"
        }
      ],
      "title": "Recording stop latency p99",
      "type": "timeseries"
    }
  ],
  "preload": false,
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["xenon", "recording"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Xenon — Recording Health",
  "uid": "xenon-recording",
  "version": 1
}
